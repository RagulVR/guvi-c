#include<stdio.h>
int main(void)
{
    char a;
    printf("Enter the character");
    scanf("%c",&a);
    if(a=='A' && a=='Z' || a=='a' && a=='z')
    {
        printf("The character is alphabet");
    }
    else
    {
        printf("The character is not alphabet");
    }
    return(0);
}
